package org.example;

public class ManagerTask {

    static void optionTask(String commands){
        if(commands.startsWith("add")){
            optionAdd(commands);
        }
    }

    private static void optionAdd(String commands) {
        
    }
}
